spring:
  datasource:
    url: jdbc:h2:mem:fintech_lab_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

payment-lab:
  provider:
    demo-signature: local-demo-signature
